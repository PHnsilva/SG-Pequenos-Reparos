# Nome da Aplicação
spring.application.name=sg-pequenos-reparos

# Configurações de Conexão com o Banco de Dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/sg_reparos
spring.datasource.username=sg_user
spring.datasource.password=sg_password
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do JPA e Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Configuração de JWT
# Chave secreta gerada com 256 bits (32 bytes), base64
jwt.secret=6v3TyEgjUcQRnWuIhjdT9QSe7XtbG8jhNNuFQKLvTgY=
# Tempo de expiração em milissegundos (30 minutos = 1800000 ms)
jwt.expiration=1800000

# Configurações do Servidor (opcional)
 server.port=8081
